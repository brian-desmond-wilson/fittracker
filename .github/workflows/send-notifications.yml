name: Send Supabase Notifications

on:
  schedule:
    - cron: "*/1 * * * *"
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase edge function
        run: |
          curl -sS -X POST \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            https://tffxvrjvkhpyxsagrjga.functions.supabase.co/send-notifications
